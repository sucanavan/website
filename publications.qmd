---
title: "Publications"
format:
  html:
    page-layout: full
    toc: false
    css: styles.css
execute:
  echo: false
---

<div class="pub-layout">
  <div class="toc-like">
    <h3>About this list</h3>
    <p>
      Citation counts are pulled from <strong>Google Scholar</strong>.<br>
      Bibliographic metadata (title, year, journal, type) comes from <strong>ORCID</strong><br>
      ( iD 0000‑0002‑7972‑7928).<br>
      <em>Data last refreshed:</em> 06 May 2025
    </p>
  </div>

  <div class="pub-table">

  ## Publications

  ```{r}
  library(readr)
  library(DT)
  library(stringr)

  # Read publications data
  pubs <- read_csv("publications.csv", show_col_types = FALSE)

  # Clean and rename columns
  pubs_display <- pubs[, c("Year", "Authors", "Title", "Journal", "Citations")]
  colnames(pubs_display) <- c("Year", "Authors", "Title", "Journal", "Citations")

  # Highlight “S Canavan” in author lists
  pubs_display$Authors <- str_replace_all(
    pubs_display$Authors,
    fixed("S Canavan"),
    "<strong>S Canavan</strong>"
  )

  # Create interactive table
  datatable(
    pubs_display,
    escape = FALSE,
    options = list(
      pageLength = 15,
      autoWidth = TRUE,
      order = list(list(0, 'desc'))
    ),
    rownames = FALSE
  )
  ```
 